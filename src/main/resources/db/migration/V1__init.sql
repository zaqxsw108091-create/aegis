-- V1__init.sql
-- Aegis 초기 베이스라인 (P1 프로덕션 기반 인프라).
--
-- 아직 도메인 엔티티가 없으므로 의도적으로 빈 베이스라인이다.
-- 이 마이그레이션의 목적은 Flyway 스키마 이력 테이블을 생성하고
-- 이후 버전(V2, V3 ...)의 기준점을 세우는 것이다.
--
-- 실제 테이블(예: users, security_event, blocked_ip 등)은
-- P2 인증 단계부터 새 버전 마이그레이션 파일로 추가한다.
-- H2(dev)/PostgreSQL(prod) 양쪽에서 동작하도록 표준 SQL을 사용할 것.
