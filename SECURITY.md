# 보안 정책 · 범위와 한계 (Security Policy / Scope & Limitations)

Aegis는 **방어형(defensive)** 웹 보안 시스템이다. 이 문서는 이 도구가 다루는 범위, 다루지 않는 것,
그리고 책임 있는 사용 방법을 명시한다. (보안 **통제 구현** 정책은 [docs/SECURITY.md](docs/SECURITY.md) 참조)

## 이 도구가 하는 일 (In scope)
- 애플리케이션 **방어**: 인증/인가, 계정 잠금, 보안 헤더, CSRF, 입력 검증, 출력 인코딩.
- **탐지**: 무차별 대입(IP·계정 단위) 집계 및 자동 차단, 요청 레이트 리미팅.
- **감사·기록**: 보안 이벤트(로그인/실패/차단/권한거부)를 변경 불가(append-only) 감사 로그로 기록.
- **모니터링**: 관리자 대시보드 및 Prometheus 메트릭으로 현황 가시화.

## 이 도구가 하지 않는 일 (Out of scope / 한계)
- **공격·침투 도구가 아니다.** 익스플로잇, 취약점 스캐닝, 공격 트래픽 생성 등을 수행하지 않는다.
- **보안 감사/침투 테스트를 대행하지 않는다.** 본 프로젝트는 보안 컨트롤의 *구현 예시*이며,
  제3자에 의한 공식 보안 감사나 모의침투(penetration test)를 거치지 않았다.
- **완전한 보안을 보장하지 않는다.** WAF, DDoS 완화, 비밀 관리(Vault/KMS), 침입 방지 시스템(IPS),
  로그 집중화/SIEM, 키 회전 등은 범위 밖이며 별도 인프라로 보완해야 한다.
- 레이트 리미터/IP 차단은 기본적으로 **단일 인스턴스(in-memory)** 기준이다. 분산 환경에서는
  추상화된 인터페이스(`RateLimiter`)를 분산 백엔드 구현으로 교체해야 한다.
- IP 식별은 `remoteAddr`(프록시 뒤에서는 forwarded-headers) 기준이며, 신뢰할 수 없는 프록시
  환경에서는 헤더 위조 가능성을 별도로 고려해야 한다.

## 책임 있는 사용 (Responsible use)
- 본인이 **소유하거나 명시적 권한이 있는** 시스템에만 배포·운영한다.
- 운영 전 반드시: `AEGIS_JWT_SECRET`·DB 비밀번호를 강한 값으로 교체, dev 기본 시크릿/H2 콘솔 비활성,
  HTTPS 종단, Actuator 접근 통제, 감사 로그 보관·접근 통제 정책 수립.
- 감사 로그에는 비밀번호·토큰 등 민감정보를 기록하지 않는다(설계상 actor/detail에 평문 시크릿 금지).
- 이 코드는 학습·참조용 기반이며, 실제 운영 적용 시 조직의 보안 요구사항에 맞춘 검토가 필요하다.

## 취약점 신고 (Reporting)
보안 취약점을 발견하면 공개 이슈로 올리기 전에 메인테이너에게 비공개로 알린다.
재현 절차와 영향 범위를 포함하면 빠른 대응에 도움이 된다.
